// Ensure transfers are initially enabled
// Ensure transfers cannot be enabled when not disabled
// Ensure transfers can be disabled once
// Ensure transfers cannot be disabled twice in succession
// Ensure transfers are not functional when disabled
// Ensure UNI pool cannot be traded agains when transfers are disabled
// Ensure transfers can be enabled after disabled
// Ensure transfers are fully functional when enabled
// Ensure transfers cannot be disabled again
// Ensure UNI pool can be traded with once transfers are enabled